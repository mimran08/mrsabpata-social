# System Health Audit — 2026-06-04

Ran a full read-only audit overnight while you were sleeping. No commits, no posts, no platform mutations. Nothing here changed cron behavior — tomorrow's 9am and 6pm cron will fire normally.

## TL;DR

**System is healthy.** Tomorrow's 9am will be the first production cron with today's stack of commits (`4b3186d` workflow env, `908a4a0` ElevenLabs cleanup, `c6cdc0b` dead-code removal). I'm confident it'll work cleanly based on tonight's smoke tests.

Found 8 issues to discuss — none are blocking, none change today's cron behavior. Categorized by priority below.

---

## 🔴 P0 — Real bugs / could break a cron

**None found.** All P0 items from earlier this week (Pollinations 402, post-bank X length, music CID, news supply, X account drift) are already fixed and shipped.

---

## 🟡 P1 — Latent issues / small wins

### 1. Migrationsverket news source is dead (HTTP 404)
- `https://www.migrationsverket.se/English/About-the-Migration-Agency/News-archive.html` returns 404
- It's been silently failing for who-knows-how-long; The Local + Schengen Visa Info pick up the slack
- **Fix**: remove from `utils/news-fetcher.ts` SOURCES array, or find the new URL on migrationsverket.se
- **Effort**: 5 min if removing, 15 min if finding replacement
- **Risk if ignored**: zero, just dead code making fetch logs noisier

### 2. cron-worker breaks the root `tsc --noEmit`
- `cron-worker/src/index.ts` references Cloudflare Workers types (`ScheduledController`, `ExecutionContext`, `cron`) that the root `tsconfig.json` doesn't have
- Root `tsc --noEmit` reports 3 errors; cron-worker itself builds fine via its own toolchain
- **Fix**: add `cron-worker` to `tsconfig.json` exclude
- **Effort**: 1 min
- **Risk if ignored**: zero, but type-check is noisier

### 3. `scripts/delete-x-tweet.ts` has a type assertion issue
- The cookie array cast `as Parameters<typeof context.addCookies>[0]` doesn't quite line up since I tweaked the formatting helper
- Script still runs (we used it tonight) — TypeScript just complains
- **Fix**: simpler explicit cast or restructure the cookie loader
- **Effort**: 5 min
- **Risk if ignored**: zero, runtime works

---

## 🟢 P2 — Cleanup / tidiness

### 4. Dead GitHub Secrets (7 of them)

These secrets exist on GitHub but are NOT referenced by any workflow:

| Secret | Why dead |
|---|---|
| `CHANNEL_NAME` | Now hardcoded as workflow env (`CHANNEL_NAME: MrSabPata`) |
| `OWNER_NAME` | Hardcoded as workflow env |
| `TIMEZONE` | Hardcoded as workflow env |
| `NEXT_PUBLIC_CHANNEL_NAME` | Next.js web app config (unused) |
| `NEXT_PUBLIC_SITE_URL` | Next.js web app config (unused) |
| `X_OAUTH2_CLIENT_ID` | X OAuth2 attempt that was abandoned |
| `X_OAUTH2_CLIENT_SECRET` | Same — abandoned OAuth2 path |

**Fix**: `gh secret delete <name>` for each
**Effort**: 2 min total
**Risk if ignored**: zero, just secret-list clutter

### 5. Legacy cookie secrets (5 more)

Older approach where session cookies were injected via secrets:
- `INSTAGRAM_COOKIES`, `TIKTOK_COOKIES`, `YOUTUBE_COOKIES`, `X_COOKIES`, `YOUTUBE_SESSION`

These ARE referenced by the legacy `.github/workflows/morning-post.yml` and `evening-post.yml` cloud workflows — but those workflows are `workflow_dispatch` only (no schedule) so they never auto-fire. We moved to self-hosted Mac runners weeks ago.

**Two paths**:
- (a) Delete the legacy cloud workflows + these 5 cookie secrets together (~5 min total)
- (b) Keep them as a "break-glass" backup in case the Mac runner ever fails (the cookies in secrets are stale anyway — they'd need refreshing before use)

I'd vote (a) because the cookies are too old to be useful as a backup, and we have local cookie extraction scripts that work better.

### 6. Legacy renderers (`utils/video-gen.ts` + `utils/video-gen-animated.ts`)

`video-gen.ts` (20s static-image-with-zoom) and `video-gen-animated.ts` (20s animated single-scene) are the fallback chain for when `generateLongVideo` fails. They haven't been used in production for ~7 days since `LONG_VIDEO=1` shipped.

**Two paths**:
- (a) Keep them as graceful fallback — currently triggered only if `LONG_VIDEO !== "1"` OR if long-video render throws
- (b) Delete and let the cron fail loudly if long-video breaks

(a) is safer. Leaving as-is is fine.

### 7. ElevenLabs reference cleanup leftovers

The `voicePart` field is gone, `voiceText` field is gone, `generateVoiceover` is gone — but the recovery scripts (`post-recovery-citizenship.ts`, `post-recovery-predeparture.ts`, `post-recovery-shortage.ts`) are now committed and may have stale-looking imports. They were one-off scripts; they ran cleanly today.

**Risk if ignored**: zero, these are historical artifacts

---

## ✅ Verified working

- **Static**: `tsc --noEmit` clean except cron-worker (P1 #2), `delete-x-tweet.ts` (P1 #3)
- **Script-dict** derivation valid for all 4 pillars (1–4)
- **Pixabay Images API**: 200, full quota
- **Pixabay Music CDN**: 200 on track URLs
- **Groq API**: 200, model listing works
- **YouTube OAuth refresh**: token returned successfully
- **The Local Sweden**: 200, scraping works
- **Schengen Visa Info**: 200, scraping works
- **Migrationsverket EN**: 404 (P1 #1)
- **post-bank.json**: all 28 entries valid, all X fields ≤245 chars, no missing fields
- **posted-news.json**: 15 entries (all within 14d window), oldest May 22, newest June 3 (today 6pm)
- **Music library**: 6 tracks (2 ambient + 2 cultural + 2 inspirational)
- **Brand assets**: `brand-logo.png` (42KB), `tiktok-app-icon.png` (47KB) both present
- **Cookies refreshed tonight**: x/tiktok/instagram/youtube all show "logged in" via extract-cookies.py
- **Cron infrastructure**:
  - `pmset` wake at 08:54 daily + ad-hoc 17:54 scheduled
  - LaunchDaemon `/Library/LaunchDaemons/com.mrsabpata.eveningwake.plist` present
  - `~/mrsabpata-wake/arm-evening-wake.sh` present
  - Cloudflare Worker cron: `0 7,16 * * *` UTC (correct: 9am + 6pm Stockholm)
  - GH self-hosted runner present
  - `PIXABAY_API_KEY` secret added today (2026-06-03 20:01 UTC)
- **Background variety**: 6 unique MD5s for 6 different seeds at same pillar → genuine variety in production
- **YouTube channel state**: 4 visible uploads match 2-day cron pattern, no orphans from today's recovery deletes

---

## Recommended order for tomorrow (if you want to clean up)

The system runs fine without any of these. Pick what feels worth doing:

1. **5 min** — Fix the 3 P1 items: remove dead Migrationsverket source, exclude cron-worker from tsconfig, fix delete-x-tweet cast. Net: clean `tsc --noEmit`.
2. **5 min** — Delete the 7 dead GH secrets (P2 #4). Net: cleaner secret list.
3. **5 min** — Delete legacy cloud workflows + 5 cookie secrets (P2 #5a). Net: cleaner workflow list.
4. **Leave alone** — P2 #6 (legacy renderers) and #7 (recovery scripts).

OR — easier — wait for tomorrow's 9am cron to confirm the production stack works end-to-end with today's commits, then decide whether to spend any time on cleanup at all.

---

*Audit generated 2026-06-04 by Claude. No state was modified during the audit. To regenerate, run the smoke tests in /tmp/smoke-*.ts and the audit commands in this conversation.*
